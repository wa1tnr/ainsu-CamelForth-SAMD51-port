extern char print_string[stack_buffer_length];

extern int input_integer;

// extern void call_me_now_ms_cleo(void);

extern void cmf_itoa(int n, char s[]);

// extern uint8_t *buf;

#include "usart_gateway.h"
#include <string.h>
